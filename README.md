# members-only-project

# Moto Club Web Forum Application

### ENV variables guide

DATABASE_URL=postgresql:[postgres url connection string]
SESSION_TABLE_NAME=[session table name]
COOKIE_SECRET=[session secret]
PORT=[server port]
