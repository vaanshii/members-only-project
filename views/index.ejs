<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>

<body class="bg-zinc-950 text-zinc-300">
  <div x-data="{ openRegister:<%= locals.openRegister === true ? 'true' : 'false' %>, 
                openLogin: <%= locals.openLogin=== true ? 'true' : 'false' %>, 
                openProfile: false,
                openCreatePost: <%= openCreatePost === true ? 'true' : 'false' %>,
                }" class="min-h-screen bg-zinc-950">

    <%- include('./partials/navbar') %>

    <% if (locals.isAuthenticated) { %>
    <%- include('./partials/createPostBtn') %>
    <%- include('./components/createPostModal') %>

    <div class="max-w-2xl mx-auto px-4 mb-6 flex flex-col gap-6">
      <% postMessages.forEach(postData => { %>
      <%- include('./components/postCard', { postData }) %>
      <% }) %>
    </div>
    <% } %>

    <% if (!locals.isAuthenticated) { %>
    <%- include('./components/registerModal', {errors: errors, formData: formData}) %>
    <%- include('./components/loginModal') %>
    <% } %>


  </div>
</body>

</html>